{
  "name": "aws-lambda",
  "version": "0.4.1",
  "private": true,
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rimraf lib && tsc",
    "start": "rimraf ./lib && tsc && node -e \"require('./lib/index').handler().then(console.log);\""
  },
  "dependencies": {
    "sectxt": "^0.4.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.80"
  }
}
